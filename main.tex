\documentclass[11pt]{article}

% =====================================================================
% XeLaTeX + Hebrew setup
% =====================================================================
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{hebrew}
\setotherlanguage{english}
\setmainfont{David CLM}
\newfontfamily\hebrewfont[Script=Hebrew]{David CLM}

% =====================================================================
% PACKAGES
% =====================================================================
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{geometry}
\usepackage{indentfirst}
\usepackage{circuitikz}
\usetikzlibrary{arrows.meta}

% =====================================================================
% PAGE GEOMETRY
% =====================================================================
\geometry{
  a4paper,
  top=2.5cm, bottom=2.5cm,
  left=2.5cm, right=2.5cm
}
\parskip 2mm

% =====================================================================
% CAPTION STYLE
% =====================================================================
\captionsetup{
  justification=justified,
  singlelinecheck=false,
  font=small,
  labelsep=endash,
}
\renewcommand{\figurename}{איור}

% Structured caption label helper
\newcommand{\caplabel}[2]{\textbf{(#1) #2:}\ }

% =====================================================================
% DOCUMENT
% =====================================================================
\begin{document}

% ----- Title block ---------------------------------------------------
\begin{center}
  {\LARGE \textbf{(77335) מעבדה בפיזיקה ב-2 $|$ ניסוי 1: כאוס}}\\
  \vspace{0.5cm}
  {\large שקד סוקניק \quad ת"ז: 313566895}\\
  {\large ניר כהן \quad ת"ז: 209720838}\\
  \vspace{0.3cm}
  {\normalsize \today}
\end{center}

\hrule
\vspace{0.5cm}

% =====================================================================
% §1  מטרת הניסוי
% =====================================================================
\section*{מטרת הניסוי}

ניסוי זה בוחן את התנהגות הדיודה 1N4005 דרך מדידת האופיין שלה (עקומת ה-I-V, בהתאמה לנוסחת שוקלי),
ואת מעגל ה-RLD כמערכת דינמית לא-לינארית המפגינה מעבר לכאוס.

\vspace{0.5cm}

% =====================================================================
% §2  מערכת הניסוי
% =====================================================================
\section*{מערכת הניסוי}

\begin{figure}[H]
  \centering
  \begin{english}
  \begin{circuitikz}[american, scale=0.85, font=\small]
    \draw (0,0) to[sV, l=$V_{\!\text{gen}}$] (0,2)
      -- (1.5,2) to[R, l=$R$, a={$470\,\Omega$}] (4.5,2)
      to[D, l=1N4005] (7.5,2) -- (7.5,0) -- (0,0);
    \draw (0,0) node[ground]{}; \draw (7.5,0) node[ground]{};
    \draw[dashed,gray] (1.5,2) -- (1.5,1) node[below]{\footnotesize Ch2};
    \filldraw[gray] (1.5,2) circle (2pt);
    \draw[dashed,gray] (7.5,2) -- (6.8,1) node[below]{\footnotesize Ch1};
    \filldraw[gray] (7.5,2) circle (2pt);
  \end{circuitikz}
  \end{english}
  \caption{\textbf{שבוע 1 — מעגל נגד-דיודה שבאמצעותו מדדנו את האופיין של הדיודה.}\\
  $I_D = (V_{\text{Ch2}} - V_{\text{Ch1}})/R$.
  על מנת לאפיין את הדיודה, שינינו ידנית את האמפליטודה של מחולל האותות ומדדנו את האופיין בשתי שיטות:
  (1) באמצעות גל ריבועי, עם כ-100 אלף דגימות בחלון הממוצע ($\SI{40}{\mu\second}$ בקצב $\SI{2.5}{GS/s}$),
  כאשר החלקים היציבים של הגל שימשו לחישוב הזרם העובר בדיודה.
  (2) באמצעות גל משולש ושימוש בממוצע נע לחישוב עקומת האופיין
  (ראו איור~\ref{fig:iv-triangle} בתוצאות).
  כדי לאמוד את קיבול הדיודה הוספנו offset שלילי ועבדנו תחת ממתח אחורי;
  מעקומת הפריקה של הדיודה חילצנו את קבוע הזמן $\tau$, ומכאן קיבלנו את הקיבול $C_j=\tau/R$.}
  \label{fig:circuit-week1}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{english}
  \begin{circuitikz}[american, scale=0.85, font=\small]
    \draw (0,0) to[sV, l=$V_{\!\text{gen}}$] (0,2)
      -- (1.5,2) to[R, l=$R$, a={$1\,\text{k}\Omega$}] (4,2)
      to[L, l=$L$, a={$100\,\text{mH}$}] (6.5,2)
      to[D, l=1N4005] (9.5,2) -- (9.5,0) -- (0,0);
    \draw (0,0) node[ground]{}; \draw (9.5,0) node[ground]{};
    \draw[dashed,gray] (1.5,2) -- (1.5,1) node[below]{\footnotesize Ch2};
    \filldraw[gray] (1.5,2) circle (2pt);
    \draw[dashed,gray] (9.5,2) -- (8.8,1) node[below]{\footnotesize Ch1};
    \filldraw[gray] (9.5,2) circle (2pt);
  \end{circuitikz}
  \end{english}
  \caption{\textbf{שבוע 2 — מעגל RLD.}\\
  בנינו מעגל המורכב ממחולל אותות, נגד ($R = \SI{1}{\kilo\ohm}$), סליל ($L = \SI{100}{\milli\henry}$)
  ודיודת סיליקון (1N4005) המחוברים בטור, כאשר הדיודה שימשה בפועל גם בתור קבל לא-לינארי.
  כדי לאפיין את התנהגות הלא-לינארית של המעגל ולבנות את מפת הביפורקציות,
  מצאנו תחילה את תדירות הרזוננס — כ-$\SI{34}{\kilo\hertz}$ —
  על ידי שינוי ידני של תדר מחולל האותות עד שמתח הדיודה הגיע למקסימום.
  לאחר מכן, בתדירות הרזוננס, שינינו ידנית את האמפליטודה בצעדים מ-200\,mV עד 3570\,mV;
  בכל ערך המתנו שהמעגל יתייצב ולאחר מכן חילצנו את ערכי הפיקים (שיאי המתח הנופלים על הדיודה)
  כדי לבנות את מפת הביפורקציות הדיסקרטית (ראו איור~\ref{fig:bif-both} בתוצאות).
  לבניית המפה הרציפה, נקטנו בגישה יעילה יותר: יצרנו אות AM עם גל נושא ב-$\SI{34}{\kilo\hertz}$,
  תדר מודולציה של $\SI{0.7}{\hertz}$ ואמפליטודה מקסימלית של $\SI{7}{\volt}$ —
  כך שהאמפליטודה עלתה וירדה במהלך מדידה אחת בת כשנייה בודדה,
  כאשר את הפיקים מדדנו לאורך כל הזמן (ראו איור~\ref{fig:bif-both} בתוצאות).}
  \label{fig:circuit-week2}
\end{figure}

\vspace{0.5cm}

% =====================================================================
% §3  תוצאות
% =====================================================================
\section*{תוצאות}

% ----- Figures 3+4: I-V plots on same page --------------------------
\noindent
\begin{minipage}{\textwidth}
  \centering
  \includegraphics[width=\textwidth]{figures/graph1IV.png}
  \captionof{figure}{%
    \textbf{עקומת האופיין של דיודה (גל ריבועי, דיסקרטי).}
    הגרף מציג את עקומת האופיין I-V הדיסקרטית של דיודת סיליקון 1N4005,
    שנמדדה באמצעות גל ריבועי ב-14 ערכי אמפליטודה ($V_{pp} = 0.05$–$10\,\si{\volt}$);
    למפרט מערכת המדידה ראו איור~\ref{fig:circuit-week1}.
    בשבע המדידות עם האמפליטודות הגבוהות ביותר ($V_{pp} = 2.4$–$\SI{10}{\volt}$, המוצגות בצהוב עד אדום כהה),
    כיוונו את האוסילוסקופ לטווח מתחים רחב יותר על מנת להכניס את הגל ה“מלא” למסך —
    דבר שהוריד משמעותית את דיוק מדידת המתח, הנראה בברור ב-errorbars האופקיים הרחבים של נקודות אלו.
    כתוצאה מכך, מדידות אלו היו בעלות אי-וודאות גבוהה מדי כדי למקמן אותן על העקומה,
    ולכן הוצאו מההתאמה לנוסחת שוקלי.
    7 הנקודות הנותרות הותאמו למשוואת שוקלי
    $I(V_D) = I_S\!\left(e^{V_D/nV_T}-1\right)$
    עם $V_T = \SI{25.85}{\milli\volt}$,
    ונתנו ערכי $n = 1.57 \pm 0.023$
    ו-$I_S = (0.90 \pm 0.19)\,\si{\nano\ampere}$,
    עקביים עם הטווח הצפוי לסיליקון על פי נוסחת שוקלי ($n \in [1,2]$).
    שגיאות המתח מחושבות כסטיית התקן של מדידות האוסילוסקופ בתוך חלון הדגימה;
    שגיאות הזרם מחושבות בהעברת שגיאות לפי
    $I = (V_{\text{Ch2}} - V_{\text{Ch1}}) / \SI{470}{\ohm}$.
  }
  \label{fig:iv-discrete}
\end{minipage}

\vspace{1em}

\noindent
\begin{minipage}{\textwidth}
  \centering
  \includegraphics[width=\textwidth]{figures/graph2IV_Triangle_Band_Zoom.png}
  \captionof{figure}{%
    \textbf{עקומת האופיין של הדיודה (גל משולש, רציף).}
    הגרף מציג את עקומת האופיין I-V הרציפה של הדיודה שנמדדה באמצעות גל משולש
    (ראו איור~\ref{fig:circuit-week1}).
    כדי לאמוד את אי-הוודאות, השתמשנו בפס סביב העקומה המשקף את הפיזור בין מחזורי מדידה חופפים.
    משוואת שוקלי $I(V_D) = I_S\!\left(e^{V_D/nV_T}-1\right)$ הותאמה לאזור הממתח הקדמי, ומהפיט קיבלנו ערכי התאמה $n = 1.5$ ו-$I_S = 0.44\,\si{\nano\ampere}$,
    אשר עקביים עם $n = 1.57 \pm 0.023$ שהתקבל במדידה הדיסקרטית
    (איור~\ref{fig:iv-discrete}).
  }
  \label{fig:iv-triangle}
\end{minipage}

% ----- Figure 5: C(V) ------------------------------------------------
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/graph3C_V_better.png}
  \caption{%
    \textbf{קיבול הדיודה כפונקציה של המתח.}
    הגרף מציג את קיבול הדיודה כפונקציה של המתח, כפי שנמדד על ידי חילוץ קבוע הזמן $\tau$
    מעקומת הפריקה של הדיודה (ראו איור~\ref{fig:circuit-week1}).
    הנתונים הותאמו למודל $C(V) = a/\!\sqrt{|V|}+c$,
    ונתנו ערכי $a = (6.0 \pm 0.6)\,\si{\pico\farad\cdot\volt^{1/2}}$
    ו-$c = (200.0 \pm 0.4)\,\si{\pico\farad}$.
    אי-הוודאות ב-$C$ הועברה מאי-הוודאות ב-$\tau$ לפי $\sigma_C = \sigma_\tau/R$,
    כאשר $\sigma_\tau$ התקבל מהתאמה אקספוננציאלית לכל מחזור פריקה בנפרד.
  }
  \label{fig:cv}
\end{figure}

% ----- Figures 4+5: Discrete + Continuous bifurcation (stacked) ------------
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/graph4_bifrocation_better.png}
  \caption{%
    \textbf{הכפלת מחזור ומעבר לכאוס במעגל RLD.}
    עליון: מפה דיסקרטית של ביפורקציות הכפלת מחזור במעגל (ראו איור~\ref{fig:circuit-week2});
    ניתן לראות את הכפלת המחזור הראשונה (ממחזור 1 למחזור 2) סביב \SI{1}{\volt},
    וביפורקציה שנייה (ממחזור 2 למחזור 4) סביב $3.3$--$3.5\,\si{V}$,
    ולאחריהן הדינמיקה הופכת כאוטית;
    צפיפות הנקודות הנמוכה מגבילה את רזולוציית המבנה הדק.
    תחתון: מפה רציפה של אותן ביפורקציות;
    הצפיפות הגבוהה מאפשרת לראות את מבנה הביפורקציות באבחנה רבה יותר.
  }
  \label{fig:bif-both}
  \includegraphics[width=\textwidth]{figures/graph5_bifurcation_continuous_nir.png}
\end{figure}

\vspace{0.5cm}

% =====================================================================
% §4  מסקנות
% =====================================================================
\section*{מסקנות}
\begin{itemize}
  \item ערכי ה-n מהגל הריבועי ($n = 1.57 \pm 0.023$) והמשולש ($n = 1.5$) עקביים זה עם זה ועם התיאוריה ($n \in [1,2]$ לסיליקון).
  \item נפילת המתח על הדיודה קטנה, ולכן כשהגדלנו את טווח המתחים איבדנו את הרזולוציה כדי לדגום את המתח בדיוק גבוה מספיק.
  \item בהתאם לתיאוריה, ראינו כי מעגל ה-RLD מכפיל מחזור ככל שהאמפליטודה עולה.
  \item מעברי הביפורקציות (כ-\SI{1}{\volt} וכ-$3.3$--$3.5\,\si{\volt}$) עקביים בין המפה הדיסקרטית לרציפה.
  \item במדידה בודדת של בערך שנייה, שיטת ה-AM מאפשרת לבנות מפת ביפורקציות שלמה ולגלות מבנה עדין יותר מהגישה הדיסקרטית.
\end{itemize}

\end{document}