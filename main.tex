\documentclass[11pt]{article}

% =====================================================================
% XeLaTeX + Hebrew setup
% =====================================================================
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{hebrew}
\setotherlanguage{english}
\setmainfont{FreeSerif}
\newfontfamily\hebrewfont[Script=Hebrew]{FreeSerif}

% =====================================================================
% PACKAGES
% =====================================================================
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{geometry}
\usepackage{tikz}
\usepackage{indentfirst}
\usepackage{xcolor}
\usepackage{array}

% Option A
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}

% Option B
\usepackage{mdframed}

% =====================================================================
% PAGE GEOMETRY
% =====================================================================
\geometry{
  a4paper,
  top=2.5cm, bottom=2.5cm,
  left=2.5cm, right=2.5cm
}
\parskip 2mm

% =====================================================================
% OPTION A — tcolorbox figure environment
%
% Usage:
%   \begin{figboxA}{fig:label}{כותרת}
%     \includegraphics[width=\linewidth]{...}
%     \fcap{תופעה}{משוואה}{פרמטרים}{שגיאה}
%   \end{figboxA}
% =====================================================================
\definecolor{headerblue}{RGB}{30, 60, 100}

\newcounter{figA}
\newcommand{\fcap}[4]{%
  \vspace{6pt}
  \begin{tabular}{@{} >{\bfseries}p{2cm} p{\dimexpr\linewidth-2.4cm-2\tabcolsep} @{}}
    (א) תופעה:   & #1 \\[4pt]
    (ב) התאמה:   & #2 \\[4pt]
    (ג) פרמטרים: & #3 \\[4pt]
    (ד) שגיאה:   & #4 \\
  \end{tabular}
}

\newenvironment{figboxA}[2]{%
  \refstepcounter{figA}%
  \label{#1}%
  \begin{tcolorbox}[
    enhanced,
    breakable,
    colback=white,
    colframe=headerblue,
    coltitle=white,
    colbacktitle=headerblue,
    fonttitle=\bfseries\large,
    title={#2},
    top=6pt, bottom=6pt, left=6pt, right=6pt,
  ]
  \centering
}{%
  \end{tcolorbox}
  \vspace{6pt}
}

% =====================================================================
% OPTION B — mdframed light border
%
% Usage:
%   \begin{figboxB}{כותרת}{תופעה}{משוואה}{פרמטרים}{שגיאה}
%     \includegraphics[width=\linewidth]{...}
%   \end{figboxB}
% =====================================================================
\mdfdefinestyle{lightframe}{
  linewidth=0.8pt,
  linecolor=gray!60,
  backgroundcolor=white,
  innertopmargin=8pt,
  innerbottommargin=8pt,
  innerleftmargin=8pt,
  innerrightmargin=8pt,
  skipabove=8pt,
  skipbelow=8pt,
}

\newenvironment{figboxB}[5]{%
  \begin{mdframed}[style=lightframe]
  \centering
  \def\captitleB{#1}%
  \def\captoefaB{#2}%
  \def\captaameB{#3}%
  \def\capparamB{#4}%
  \def\capshagiB{#5}%
}{%
  \par
  \vspace{6pt}
  {\color{gray!60}\hrule height 0.4pt}
  \vspace{6pt}
  \begin{flushleft}
    \textbf{\captitleB}\\[4pt]
    \begin{tabular}{@{} >{\itshape}p{2cm} p{\dimexpr\linewidth-2.4cm-2\tabcolsep} @{}}
      (א) תופעה:   & \captoefaB \\[4pt]
      (ב) התאמה:   & \captaameB \\[4pt]
      (ג) פרמטרים: & \capparamB \\[4pt]
      (ד) שגיאה:   & \capshagiB \\
    \end{tabular}
  \end{flushleft}
  \end{mdframed}
  \vspace{6pt}
}

% =====================================================================
% OPTION C — standard figure with structured caption labels
%
% Usage: standard \begin{figure}[H] with \caption{\caplabel{...}...}
% =====================================================================
\newcommand{\caplabel}[1]{\textbf{(#1)}\ }

% =====================================================================
% DOCUMENT
% =====================================================================
\begin{document}

% ----- Title block ---------------------------------------------------
\begin{center}
  {\LARGE \textbf{(77335) מעבדה בפיזיקה ב-2 $|$ ניסוי 1: כאוס}}\\
  \vspace{0.5cm}
  {\large שקד סוקניק \quad ת"ז: 313566895}\\
  {\large ניר כהן \quad ת"ז: 209720838}\\
  \vspace{0.3cm}
  {\normalsize \today}
\end{center}

\hrule
\vspace{0.5cm}

% =====================================================================
% §1  מטרת הניסוי
% =====================================================================
\section*{מטרת הניסוי}

ניסוי זה בוחן את התנהגות הדיודה 1N4005 דרך מדידת עקומת ה-I-V וקיבול המעבר,
ואת מעגל ה-RLD כמערכת דינמית לא-לינארית המפגינה הכפלת-מחזור ומעבר לכאוס.
המטרה היא לאמת את משוואת Shockley, למדוד את תלות $C_j(V)$, ולאפיין את מפת
הבפורקציה של המעגל בהתאם לתיאוריה האוניברסלית של פייגנבאום.

\vspace{0.5cm}

% =====================================================================
% §2  מערכת הניסוי
% =====================================================================
\section*{מערכת הניסוי}

\textbf{חלק 1 — אפיון דיודה:}
מחולל פונקציות מחובר בטור עם נגד $R = \SI{470}{\ohm}$ ודיודה 1N4005.
ערוץ 1 (Ch1) מדד את מתח קצות הדיודה; ערוץ 2 (Ch2) את מתח יציאת המחולל.
הזרם חושב כ-$I_D = (V_{\text{Ch2}} - V_{\text{Ch1}}) / R$.

\textbf{חלק 2 — מעגל RLD:}
נוסף סליל $L = \SI{100}{\milli\henry}$ בטור. גל נושא בתדר $\approx\SI{34}{\kilo\hertz}$
עם מודולציית AM בתדר $\SI{0.7}{\hertz}$ ושיא $\SI{7}{\volt}$ שימש לסריקה רציפה של
משרעת הכניסה.

\vspace{0.5cm}

% =====================================================================
% §3  תוצאות
% =====================================================================
\section*{תוצאות}


% ------------------------------------------------------------------
%  OPTION B — mdframed  (Figs 2, 3)
% ------------------------------------------------------------------

\begin{figboxB}
  {איור 2 — עקומת I-V רציפה (גל משולש)}
  {עקומת מאפייני I-V של דיודת 1N4005 הנמדדת בגל משולש. הרצועה הכחולה הרחבה
   משקפת את שני כיווני הסריקה. היסט חיובי קבוע של $\approx \SI{0.3}{\milli\ampere}$
   בהטיה הפוכה הוא ארטיפקט של רמת בסיס ה-ADC.}
  {$I = I_S\!\left(e^{V_D/nV_T}-1\right)$, מותאם לאזור הטיה קדמית ($V_D > \SI{0.3}{\volt}$)}
  {$n = \text{TODO}$, \quad $I_S = \text{TODO}~\si{\ampere}$}
  {הרצועה הנראית לעין משקפת רעש; אין פס שגיאה פורמלי.}
  \includegraphics[width=\linewidth]{figures/graph2IV_Triangle.png}
\end{figboxB}

\begin{figboxB}
  {איור 3 — קיבול מעבר $C_j(V)$}
  {קיבול מעבר דיודת 1N4005 פוחת עם עליית המתח ההפוך, בהתאם לתיאוריית שכבת הניגוד.
   סך הוריאציה הנמדדת $\approx \SI{4.5}{\pico\farad}$ על בסיס $\approx \SI{200}{\pico\farad}$.}
  {$C_j(V) = a/\!\sqrt{|V|} + c$}
  {$a = \text{TODO}~\si{\farad\cdot\volt^{1/2}}$, \quad $c = \text{TODO}~\si{\farad}$}
  {אופקי: קוונטיזציית ADC; אנכי ($\sigma_\tau \to \sigma_C$): טרם נוסף.}
  \includegraphics[width=\linewidth]{figures/graph3C_V.png}
\end{figboxB}

% ------------------------------------------------------------------
%  OPTION C — standard figure + structured caption  (Fig 3 → here Fig 4)
% ------------------------------------------------------------------

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{figures/graph4_bifrocation.png}
  \caption{%
    \textbf{איור 4 — דיאגרמת בפורקציה בדידה של מעגל RLD.}
    \caplabel{א}תופעה:
    דיאגרמת בפורקציה בדידה ($R=\SI{470}{\ohm}$, $L=\SI{100}{\milli\henry}$, דיודת 1N4005)
    המציגה מפלת הכפלת-מחזור 1→2→4→כאוס ככל שמשרעת הכניסה עולה.
    גוון האפור — TODO (לבדוק ב-\textenglish{get\_cycles.py}).
    \caplabel{ב}התאמה: אין.
    \caplabel{ג}פרמטרים: אין.
    \caplabel{ד}שגיאה:
    קוונטיזציית ADC $\approx \SI{80}{\milli\volt}$ (2 V/div, 8-bit); פסי שגיאה — יש להוסיף.
  }
  \label{fig:bif-discrete}
\end{figure}

\begin{figboxA}{fig:bif-cont}{איור 5 — מפת בפורקציה רציפה (AM)}
  \includegraphics[width=\linewidth]{figures/graph5_bifurcation_cont_triangle.png}
  \fcap
    {מפת בפורקציה רציפה של מעגל RLD (נושאת $\approx\SI{34}{\kilo\hertz}$,
     $f_{AM}\approx\SI{0.7}{\hertz}$, שיא $\SI{7}{\volt}$). כחול = משרעת עולה,
     אדום = יורדת; ההפרדה מצביעה על היסטרזיס. ענף עליון: שיאי הטיה קדמית;
     ענף תחתון: שיאי הטיה הפוכה (קבועים, $\approx\SI{-3.5}{\volt}$).}
    {אין התאמה}
    {קווי מעבר (הערכה חזותית, $\pm\SI{0.1}{\volt}$):
     $1\!\to\!2 \approx \SI{3.0}{\volt}$, \quad $2\!\to\!4 \approx \SI{3.5}{\volt}$}
    {קוונטיזציית ADC $\approx \SI{80}{\milli\volt}$.}
\end{figboxA}

\vspace{0.5cm}

% =====================================================================
% §4  דיון
% =====================================================================
\section*{דיון}

% TODO

\vspace{0.5cm}

% =====================================================================
% §5  נספחים
% =====================================================================
\section*{נספחים}

% TODO

\end{document}
